// Import shared types from backend
// Note: In production, these types are compiled to JS in dist/
// During development, TypeScript resolves these paths
export { SessionStatus, ClaudeSession, ClaudeProcess } from '../../src/types/shared';
